[project]
name = "openfoodfacts-mcp"
version = "0.1.0"
description = "MCP server for Open Food Facts search API"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.2.4",
    "httpx>=0.28.1",
]

[project.scripts]
openfoodfacts-mcp = "main:mcp.run"
